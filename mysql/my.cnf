[mysqld]
server-id           = 1
log_bin             = mysql-bin
binlog_format       = ROW
binlog_row_image    = FULL
binlog_expire_logs_seconds = 259200
gtid_mode           = ON
enforce_gtid_consistency = ON
